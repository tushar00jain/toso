"""Outcome metrics and console rendering.

``summary.py`` renders the fabric/wallclock summary plus the source->dest tree.
The measurements themselves are a shared :class:`sim_common.report.Ledger` --
like ``dedup_sim``, this capability defines no metrics of its own.
"""
